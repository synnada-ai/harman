from setuptools import setup, find_packages

setup(
    name="harman", # Replace with your package name
    version="0.0.3", # Initial version
    description= "",        
    packages=find_packages(),  # Automatically include all packages in the directory
)